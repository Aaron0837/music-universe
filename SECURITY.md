# 安全策略

## 报告漏洞

**请不要用公开 issue 报告安全问题。**

请使用 GitHub 的私密漏洞报告功能：

👉 [Report a vulnerability](https://github.com/Aaron0837/music-universe/security/advisories/new)

这条通道只有仓库维护者能看到内容，便于在修复发布前保持披露克制。

## 适用范围

Music Universe 是一个纯静态的客户端应用：构建产物是静态文件，没有服务端、没有数据库服务、没有用户账号，也不发起任何网络请求。

因此绝大多数传统服务端漏洞类型（认证绕过、SQL 注入、SSRF、越权访问）在这里没有对应的攻击面。真正值得报告的是：

- **客户端代码执行** —— 导入的音频文件元数据 / 歌词文本触发的 XSS 或注入
- **本地数据越权** —— 让一个源读取到另一个源或另一个用户配置的 IndexedDB 数据
- **Service Worker 缺陷** —— 缓存投毒、把非预期内容纳入离线缓存
- **供应链问题** —— 依赖包中的已知漏洞，尤其是 `music-metadata`、`dexie`、`three` 这几个处理不可信输入或体量较大的包
- **构建 / 部署链路** —— GitHub Actions 工作流中的权限过宽或注入风险

## 已知的非问题

- **音频文件本身**：应用解码你主动导入的文件。恶意音频文件导致的浏览器崩溃属于浏览器引擎的问题，请报告给对应浏览器厂商。
- **无遥测是设计**：应用不收集任何数据，所以"它记录了用户行为"这类报告不适用。

## 响应预期

这是一个由个人维护的开源项目，没有专职安全团队。我会尽力在收到报告后一周内确认，并在确认后 30 天内给出修复或说明。修复会随下一次 main 推送部署。
