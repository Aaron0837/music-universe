# Normalise line endings: LF in the repository and in the working tree.
# Without this, core.autocrlf=true makes git rewrite every file it touches.
* text=auto eol=lf

# Binary assets are never text-normalised.
*.png binary
*.webp binary
*.jpg binary
*.ico binary
*.webm binary
*.woff2 binary